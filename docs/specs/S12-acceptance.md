# S12 集成验收与交付

前置：S08–S11 已集成。主 agent 独占最终验收与任务板状态更新。

完成条件：

1. 新前端类型/行为/构建检查与全量后端离线测试通过；旧 UI 文本断言不当作 Vue 验收。逐项记录旧保证到新测试的映射。
2. 实际浏览器通过 HTTP 验证对话、确认、学习反馈、历史/ref、管理、主题、窄屏及错误；截图人工检查。受控模型与真实模型结果分开。
3. 实际 Nginx/Compose 验证资源、JSON 错误、流式、断开、重启持久化、恢复/回退；已有用户数据保护记录可复核。
4. 代表性真实模型对话/事务/学习冒烟有实际结果；后端逻辑未改变则引用先前大规模学习评测的适用范围，不重新宣称效果提升。
5. 对外代码、功能、README、文档和打包材料不含受限制的运行时来源名称；本地内部来源映射有实际源码和差异核验。
6. README/部署文档描述实际运行方式和已知范围；记录代码/镜像身份、命令、结果、失败与限制，不能把未跑检查写成通过。

若环境阻塞某项，任务板标为 blocked 并记录复现条件与已完成证据，不能将剩余依赖连带标 done。
