# MellowDay 重构实施计划

状态：计划与目录准备完成，尚未开始功能实施。
日期：2026-09-18。

## 1. 已确认决策

- 本分支是后续重构工作的主线，属于现有 mellowday 仓库，不创建新仓库。
- 在独立工作目录实施，不修改原工作目录及其中未提交的记忆功能改动。
- 以指定参考项目的实际运行时源码为实现起点，不以旧 MellowDay 的 AgentCore 为重构起点；具体来源路径、版本及文件映射见本机 `.local-planning/SOURCE_BASELINE.md`。
- 保留 MellowDay 的产品方向和业务需求；旧实现仅作为业务行为、界面和必要服务的参考，可按新运行时需要调整。
- 优先保持参考运行时的对话循环、Skills 检索与演化、上下文压缩等机制容易与其源码对应，避免重新发明一套架构。
- 面向个人项目演示和求职讲解，不引入多租户、企业部署、复杂数据迁移及无关产品范围。
- 代码、包名、配置、界面、功能介绍、README 和其他对外材料统一使用 MellowDay 表述，不声明从参考项目重构。来源定位仅保留在不提交、不打包的本机内部记录中。已有必要的版权与许可文件不因品牌调整而删除。
- 当前阶段只准备计划和干净目录；功能代码在后续实施阶段导入。

## 2. 工作区与分支

- 仓库：沿用 origin 指向的 Ruacha00/mellowday。
- 重构分支：`refactor/mellowday-runtime`。
- Git 基线：`5e8616231053ab5df0b52a5ac96bb5e52952f553`，准备时本地 main 与 origin/main 一致。
- 新目录：`MellowDay-Rebuild`，通过 Git worktree 与原仓库共享历史和远程。
- 原目录：相邻 `ChatBot`，仍在 main，保留原有全部文件和未提交改动。
- 本分支作为实施主线不等于修改远程默认分支；完成验收后再决定合并或切换默认分支。
- 本准备提交暂不提供可运行应用；README 必须准确标注阶段。

## 3. 产品目标和范围

目标：能够持续处理个人事务、记住用户事实，并从明确纠正中学习做事方式的个人助理。

首个完整演示：读取日历与待办 → 根据用户习惯规划明天 → 用户纠正规划方式 → 更新对应 Skill → 新会话按新规则完成规划 → 对照评测确认改善。

核心范围：
1. 人格化聊天、流式回复、历史会话及模型配置。
2. 待办、提醒、日历、笔记的查询与变更。
3. 用户事实的写入、检索、更新、删除和在任务中的使用。
4. 工作流程 Skills 的检索、加载、提取、合并、版本管理及评测。
5. 长会话上下文预算、工具结果压缩、会话摘要与恢复。
6. 每日回顾和受用户设置约束的主动问候。
7. 网页管理入口、执行反馈、必要的确认和撤销。

后续扩展：MCP 工具接入、独立复盘子任务、更多模型协议。
本轮不要求：原生桌面壳、安装器、任意终端编程能力、全面复制参考项目的示例 Skills。

## 4. 目标结构

```text
frontend/                         聊天、事务管理、习惯管理、设置
src/mellowday/
  runtime/                        基于指定源码适配的 Agent 运行时
    agent.py                      对话循环与工具调度
    providers/                    模型协议与流式适配
    context/                      预算、工具结果处理、会话折叠
    skills/                       发现、检索、加载、演化、版本及评测
    tools/                        工具定义与调用适配
    sessions/                     会话状态与恢复
  personal_assistant/             待办、日历、提醒、笔记、记忆、回顾
  web_app/                        API、事件流、运行配置和生命周期
  storage/                        持久化及文件定位
  integrations/                   后续 MCP 等接入
 tests/                           随阶段新增的有效测试
 evals/                           新方案的对照评测与固定案例
```

目录可按源码依赖小幅调整；不得为了结构漂亮而大幅重写核心机制。前端和业务模块按需恢复，不整体复制旧 AgentCore。

## 5. 源码导入原则

1. 从内部记录指定的源码目录读取真实文件；先记录文件哈希、依赖和调用关系。
2. 优先导入 Agent 循环、工具协议、Skill 发现与检索、演化、上下文及会话机制。
3. 将终端打印、交互输入、确认和运行状态转换为回调或事件，接到网页接口。
4. 将路径、环境变量、包名和用户可见文本改为 MellowDay；修改引用关系并检查配置兼容性。
5. 用个人事务工具替换默认编程工具集合，不把 shell、任意文件编辑及编码示例 Skills 直接作为产品默认能力。
6. 只按依赖声明安装所需库，不复制参考项目或旧项目的环境、缓存、数据、会话、密钥、IDE 设置及生成结果。
7. 对必须改变的核心行为记录原因和验证方式，内部保留源码对应表，方便讲解。

## 6. 三类持久信息

| 类型 | 示例 | 处理规则 |
| --- | --- | --- |
| 用户事实 | 通常六点下班 | 结构化事实，带范围、来源和更新时间，可修改删除 |
| 工作流程 Skill | 先排固定日程，再安排三个重点任务 | 可检索指令，带版本、触发条件和用户反馈证据 |
| 事务记录 | 明晚七点复习 | 存入待办或日历，由相应工具管理 |

会话摘要属于会话状态，不自动成为永久事实或 Skill。人格配置独立于上述三类信息。当前明确要求优先于默认习惯。

旧记忆模块有未提交改动。实施时只读检查原工作区与基线差异，将需要的行为有选择地适配；不得切换、覆盖或提交原目录的改动。内部快照仅供核对。

## 7. 接口与执行流程

- 网页请求携带会话标识、用户消息及必要配置，运行时通过独立会话状态执行。
- 统一事件至少覆盖：回复增量、工具开始、工具结果、需要确认、完成、错误；前端不解析终端文本。
- 业务工具适配器将运行时的工具参数映射到业务服务，返回可验证的记录标识和结果。
- 已明确授权的普通内部事务按产品语义执行；涉及确认的操作由网页确认结果恢复执行，避免重复创建记录。
- Skills 候选从用户明确反馈提取，经过新增、合并或丢弃处理；事实与一次性请求不能作为永久工作流程。
- 检索先沿用源码的词项检索路线，验证中文场景效果后再决定改进；不预设引入向量数据库。
- 记忆读取保留当前事实与失效处理；更新、删除后，新会话和事务工具不能使用旧值。
- 会话折叠保留当前目标、已确认约束、已完成事项和待办事项；原始历史独立保存，不被摘要覆盖。

## 8. 分阶段实施与验收

### P0：计划、分支与目录准备（本次）

产物：本计划、阶段 README、忽略规则、本地源码映射、清理清单和旧文件归档。
验收：同一远程仓库的新分支与独立 worktree；原目录状态不变；新目录没有旧环境与临时产物；内部记录不会进入提交。

### P1：导入运行时最小闭环

- 按指定源码导入核心循环、模型调用、工具协议和会话状态。
- 解耦终端输出与输入；提供网页可消费事件。
- 配置 MellowDay 名称、包路径、数据目录及模型配置。
- 接入一个只读示例业务工具，验证自然语言 → 调用 → 回复。
- 优先跑通现有使用的 OpenAI 兼容接口，其他协议随后验证。

验收：真实模型可完成普通对话和一次工具调用；流式回复可展示；模型错误能结束当前请求；会话之间不串状态。

### P2：个人事务与界面接回

- 从归档按需恢复前端布局与业务服务，适配新接口和事件。
- 接回待办、日历、提醒、笔记及其管理页面。
- 恢复人格与模型设置、会话历史、执行结果展示。
- 替换旧启动和构建流程，只保留能实际运行的命令。

验收：聊天创建记录，管理页可见；管理页修改后聊天查询得到新值；需确认的动作在确认后执行一次；真实数据库验证结果。

### P3：事实记忆与上下文管理

- 选择性复用旧记忆业务规则及必要的未提交改进，适配新运行时。
- 接入上下文预算、工具输出压缩、摘要折叠和会话恢复。
- 将事实召回与流程 Skill 检索作为独立来源组装上下文。

验收：新会话使用正确事实；事实修改或删除后不再沿用旧值；长对话折叠后仍能完成未完成任务；当前明确指令覆盖默认偏好。

### P4：用户反馈驱动的 Skill 学习

- 接入候选提取、相似规则检索、新增/合并/丢弃、版本记录及使用信号。
- 提供习惯查看、启停、修改和恢复版本入口。
- 先实现每日规划、每日回顾、笔记整理三类流程。
- 保持“实时写入更新”和“离线评测晋级”边界清楚。参考源码不应被描述为所有写入都先通过评测；本期不额外承诺自动评测阻断上线。

验收：一次纠正改变后续新会话行为；一次性要求不会成为永久规则；重复反馈合并而非无限追加；停用或恢复规则实际影响后续执行。

### P5：评测与完整演示

- 使用固定开发案例调试，冻结后再运行保留案例。
- 对比关闭 Skill、固定 Skill、学习后 Skill；其他模型参数和初始数据一致。
- 记忆评测与 Skill 评测分别记录，完整业务流程另计。
- 记录源码版本及工作区哈希、模型实际标识、案例、原始响应、工具调用和数据库结果。
- 输出真实结果、失败案例和演示脚本，不预填提升比例。

验收指标：规则遵守率、误学习率、相关检索结果、事务参数正确率、重复写入数、上下文折叠后任务连续性、请求次数及 token 使用；每个比例说明分母。

核心案例：首次规划；纠正后再次规划；当前要求覆盖习惯；临时要求不落为永久规则；无关请求不加载规划规则；事实与习惯同时更新；规则禁用/恢复；长对话继续任务。

### P6：回顾、主动问候与可选扩展

- 接回每日回顾和已有主动问候设置及节流语义。
- 如演示确需外部工具，再适配 stdio MCP 的发现、命名和调用。
- 如复盘任务需要独立上下文，再接入子 Agent，并沿用父任务允许的业务工具范围。
- 子任务调用是否并行以实际实现为准，不把嵌套调用宣传成并行调度。

验收：回顾能关联真实事务；关闭主动问候后不触发；扩展工具能完成一个真实调用；子任务完成与失败均可在界面收敛。

## 9. 清理政策和当前结果

- 本次在新 worktree 中将旧源码、前端、测试、CI、构建配置、启动脚本和旧介绍文件移出工作根目录，统一归档；并非永久删除。
- 归档位置与逐项移动清单见 `.local-planning/CLEANUP_REPORT.md`。
- 原目录中的环境、缓存、评测报告、实验目录及个人数据原地保留，没有复制到新目录。
- 后续按阶段有选择地取回业务模块、界面和有价值的测试案例，不能整包恢复旧工程。
- 实施仍要新增必要的行为测试；清理旧测试不代表取消验证。
- 保留 LICENSE；使用旧界面资产时同时恢复对应资产说明。
- 不沿用过时 CI、依赖或启动脚本；待新运行时可运行后按实际命令重建。

## 10. 发布文档与求职材料

- README 只描述当前真实实现、运行方式及演示内容；尚未实现的能力标注计划中。
- 对外核心表述：个人助理、工具调用、个性化流程学习、上下文管理、可验证的业务闭环。
- 不对外描述参考来源；内部源码映射仅辅助开发和学习，不进入仓库提交或安装包。
- 不把持久化指令更新表述为模型训练，不把词项检索表述为向量检索。
- 最终提供架构图、一次完整请求链路、失败与改进案例、评测报告和可复现演示。

## 11. 新对话接手入口

从本目录启动，先读本计划以及 `.local-planning/SOURCE_BASELINE.md`、`.local-planning/CLEANUP_REPORT.md`，检查分支和工作区状态，然后从 P1 开始。

必须保持：参考源码为运行时基线；旧 MellowDay 实施细节可调整；产品方向不变；内部来源记录不提交；原目录未提交改动不受影响。每个阶段完成后记录实际变更、验证和剩余事项。
